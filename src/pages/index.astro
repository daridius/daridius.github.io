---
// Standalone Upload Page - Dark Wrapped Style
---

<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>WhatsApp Wrapped 2025</title>
        <link
            href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;700&display=swap"
            rel="stylesheet"
        />
        <style is:global>
            /* Dark Theme Variables */
            :root {
                --bg-dark: #0b141a; /* WhatsApp Web Dark BG */
                --bg-gradient-start: #0f2027;
                --bg-gradient-end: #203a43;

                --wa-green: #25d366; /* Neon Green */
                --wa-teal: #128c7e;

                --glass-bg: rgba(255, 255, 255, 0.05);
                --glass-border: rgba(255, 255, 255, 0.1);

                --text-primary: #e9edef;
                --text-secondary: #8696a0;
            }

            * {
                margin: 0;
                padding: 0;
                box-sizing: border-box;
            }

            body {
                font-family: "Outfit", sans-serif;
                background: radial-gradient(
                    circle at top right,
                    #10302b,
                    #0b141a 60%
                );
                color: var(--text-primary);
                min-height: 100vh;
                display: flex;
                align-items: center;
                justify-content: center;
                overflow: hidden; /* Prevent scroll */
            }

            /* Decorative Background Elements */
            .orb {
                position: absolute;
                border-radius: 50%;
                filter: blur(80px);
                z-index: -1;
                opacity: 0.4;
                animation: float 10s infinite ease-in-out;
            }
            .orb-1 {
                width: 400px;
                height: 400px;
                background: var(--wa-teal);
                top: -100px;
                right: -100px;
            }
            .orb-2 {
                width: 300px;
                height: 300px;
                background: var(--wa-green);
                bottom: -50px;
                left: -50px;
                animation-delay: 2s;
            }

            @keyframes float {
                0%,
                100% {
                    transform: translate(0, 0);
                }
                50% {
                    transform: translate(20px, 30px);
                }
            }

            .container {
                width: 100%;
                max-width: 480px;
                padding: 24px;
                text-align: center;
                z-index: 10;
            }

            /* Header */
            h1 {
                font-size: 32px;
                font-weight: 700;
                margin-bottom: 8px;
                background: linear-gradient(135deg, #fff 0%, #25d366 100%);
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
            }

            p.subtitle {
                font-size: 16px;
                color: var(--text-secondary);
                margin-bottom: 40px;
                font-weight: 300;
            }

            /* Upload Card */
            .upload-card {
                background: var(--glass-bg);
                border: 1px solid var(--glass-border);
                backdrop-filter: blur(20px);
                border-radius: 24px;
                padding: 4px; /* Padding for the border effect */
                transition:
                    transform 0.3s ease,
                    box-shadow 0.3s ease;
            }

            .upload-card:hover {
                transform: translateY(-5px);
                box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
                border-color: rgba(37, 211, 102, 0.3);
            }

            .drop-zone {
                display: flex;
                flex-direction: column;
                align-items: center;
                justify-content: center;
                padding: 48px 24px;
                border: 2px dashed rgba(255, 255, 255, 0.1);
                border-radius: 20px;
                cursor: pointer;
                transition: all 0.3s ease;
            }

            .drop-zone:hover,
            .drop-zone.drag-active {
                border-color: var(--wa-green);
                background: rgba(37, 211, 102, 0.05);
            }

            .icon {
                width: 56px;
                height: 56px;
                margin-bottom: 24px;
                color: var(--text-secondary);
                transition: color 0.3s;
            }

            .drop-zone:hover .icon {
                color: var(--wa-green);
            }

            .btn-text {
                font-size: 18px;
                font-weight: 600;
                margin-bottom: 8px;
                color: #fff;
            }

            .sub-text {
                font-size: 13px;
                color: var(--text-secondary);
            }

            /* Status */
            .status {
                margin-top: 24px;
                font-size: 14px;
                font-weight: 500;
                min-height: 24px;
                opacity: 0;
                transition: opacity 0.3s;
            }
            .status.visible {
                opacity: 1;
            }
            .status.success {
                color: var(--wa-green);
            }
            .status.error {
                color: #ff5f5f;
            }
            .status.process {
                color: var(--wa-green);
                animation: pulse 1.5s infinite;
            }

            @keyframes pulse {
                0% {
                    opacity: 0.6;
                }
                50% {
                    opacity: 1;
                }
                100% {
                    opacity: 0.6;
                }
            }

            /* Footer */
            .footer {
                margin-top: 48px;
                font-size: 12px;
                color: rgba(255, 255, 255, 0.3);
            }
            .footer strong {
                color: rgba(255, 255, 255, 0.5);
                font-weight: 500;
            }
        </style>
    </head>
    <body>
        <div class="orb orb-1"></div>
        <div class="orb orb-2"></div>

        <div class="container">
            <h1>WhatsApp Wrapped</h1>
            <p class="subtitle">Discover the story behind your chats.</p>

            <div class="upload-card">
                <label class="drop-zone" id="drop-zone">
                    <svg
                        class="icon"
                        fill="none"
                        stroke="currentColor"
                        stroke-width="1.5"
                        viewBox="0 0 24 24"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            d="M12 16.5V9.75m0 0l3 3m-3-3l-3 3M6.75 19.5a4.5 4.5 0 01-1.41-8.775 5.25 5.25 0 0110.233-2.33 3 3 0 013.758 3.848A3.752 3.752 0 0118 19.5H6.75z"
                        ></path>
                    </svg>
                    <div class="btn-text">Drop your chat file</div>
                    <div class="sub-text">Supports .txt or .zip export</div>
                    <input
                        type="file"
                        id="file-upload"
                        accept=".txt,.zip"
                        style="display: none;"
                    />
                </label>
            </div>

            <div id="status" class="status">Ready to analyze</div>

            <div class="footer">
                <strong>100% Private.</strong> Processing happens on your device.
            </div>
        </div>

        <script>
            import JSZip from "jszip";
            import { parseChat } from "../utils/chatParser";
            import { compressAndEncode } from "../utils/compression";

            const dropZone = document.getElementById("drop-zone");
            const fileInput = document.getElementById(
                "file-upload",
            ) as HTMLInputElement;
            const statusEl = document.getElementById("status");

            function setStatus(
                msg: string,
                type: "process" | "success" | "error",
            ) {
                if (statusEl) {
                    statusEl.textContent = msg;
                    statusEl.className = `status visible ${type}`;
                }
            }

            // Interaction Handlers
            if (dropZone) {
                ["dragenter", "dragover"].forEach((e) => {
                    dropZone.addEventListener(e, (ev) => {
                        ev.preventDefault();
                        dropZone.classList.add("drag-active");
                    });
                });

                ["dragleave", "drop"].forEach((e) => {
                    dropZone.addEventListener(e, (ev) => {
                        ev.preventDefault();
                        dropZone.classList.remove("drag-active");
                    });
                });

                dropZone.addEventListener("drop", (e: DragEvent) => {
                    const files = e.dataTransfer?.files;
                    if (files && files.length) processFile(files[0]);
                });
            }

            if (fileInput) {
                fileInput.addEventListener("change", (e: Event) => {
                    const target = e.target as HTMLInputElement;
                    if (target.files && target.files.length)
                        processFile(target.files[0]);
                });
            }

            async function processFile(file: File) {
                setStatus("Unpacking your story...", "process");

                try {
                    let text = "";

                    // UX Delay
                    await new Promise((r) => setTimeout(r, 600));

                    if (file.name.endsWith(".zip")) {
                        const zip = new JSZip();
                        const loaded = await zip.loadAsync(file);
                        const chatFile = Object.values(loaded.files).find(
                            (f) =>
                                f.name.includes("_chat.txt") ||
                                f.name.endsWith(".txt"),
                        );
                        if (!chatFile)
                            throw new Error("No chat file found in archive.");
                        text = await chatFile.async("string");
                    } else {
                        text = await file.text();
                    }

                    setStatus("Available 2025 memory found...", "process");
                    await new Promise((r) => setTimeout(r, 400));

                    const data = parseChat(text);

                    // Compress and Generate URL
                    const hash = compressAndEncode(data);
                    const shareUrl = `${window.location.origin}/v#${hash}`;

                    // Update UI to Success State
                    setStatus("Wrapped Ready!", "success");

                    const card = document.querySelector(".upload-card");

                    if (card) {
                        // Replace card content with Success/Share view
                        card.innerHTML = `
                        <div style="text-align: center; padding: 20px;">
                            <div style="width: 60px; height: 60px; background: #25D366; border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 24px;">
                                <svg width="32" height="32" fill="none" stroke="white" stroke-width="3" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
                            </div>
                            <h2 style="font-size: 24px; font-weight: 700; color: #fff; margin-bottom: 8px;">Analysis Complete!</h2>
                            <p style="color: var(--text-secondary); margin-bottom: 32px; font-weight: 300;">Your 2025 story is ready to be explored.</p>
                            
                            <div style="display: flex; flex-direction: column; gap: 12px; margin-bottom: 32px;">
                                <a href="/${hash}" class="btn-primary" style="display: block; background: #25D366; color: #0b141a; font-weight: 700; padding: 16px; border-radius: 16px; text-decoration: none; font-size: 16px; transition: transform 0.2s;">
                                    View My Wrapped
                                </a>
                            </div>

                            <div style="background: rgba(255,255,255,0.05); padding: 16px; border-radius: 16px; border: 1px solid rgba(255,255,255,0.1);">
                                <p style="font-size: 11px; text-transform: uppercase; color: var(--text-secondary); letter-spacing: 1px; margin-bottom: 8px; text-align: left;">Shareable Link</p>
                                <div style="display: flex; align-items: center; gap: 12px;">
                                    <input type="text" id="share-link-input" value="${shareUrl}" readonly style="flex: 1; background: transparent; border: none; color: #fff; font-family: monospace; font-size: 13px; outline: none;">
                                    <button id="copy-btn" style="background: rgba(37, 211, 102, 0.1); border: none; color: #25D366; padding: 6px 12px; border-radius: 8px; cursor: pointer; font-weight: 600; font-size: 12px; transition: all 0.2s;">COPY</button>
                                </div>
                            </div>
                        </div>
                    `;

                        // Add Copy Listener
                        document
                            .getElementById("copy-btn")
                            ?.addEventListener("click", () => {
                                const input = document.getElementById(
                                    "share-link-input",
                                ) as HTMLInputElement;
                                input.select();
                                navigator.clipboard.writeText(input.value);
                                const btn = document.getElementById("copy-btn");
                                if (btn) {
                                    btn.textContent = "COPIED!";
                                    setTimeout(() => {
                                        btn.textContent = "COPY";
                                    }, 2000);
                                }
                            });
                    }
                } catch (err) {
                    console.error(err);
                    setStatus(
                        err instanceof Error
                            ? err.message
                            : "Something went wrong.",
                        "error",
                    );
                }
            }
        </script>
    </body>
</html>
